# 项目背景

L7Audio 是一个 Android 车载音频 App，运行在 **银河 L7** 的车机上（QNX + Flyme Auto），SoC 为 **骁龙 8155（6 核）**。实时音频管线对 CPU 资源敏感，性能优化是持续关注点。

# 长期约定

## 代码注释
每次写代码都必须在关键部位加中文注释，包括但不限于：
- 类/方法职责
- 复杂逻辑说明
- 竞态条件/线程安全设计
- 异常处理和降级策略
- 性能敏感路径的说明

## 每次修改的流程

### 日常修改（每次代码/配置改动）
1. 完成功能/修复代码
2. **versionCode +1**（`app/build.gradle.kts`），versionName 不动
3. 更新 `CHANGELOG.md`：
   - 更新页头日期和 versionCode
   - 在当前版本条目下添加本次修改的描述
4. **不提交、不推送**，保持纯本地修改

### 上传/同步时
当用户说"上传"或"同步"时：
1. 汇总自上次版本名发布以来的所有改动
2. **versionName 升次版本号**（如 1.5.6 → 1.5.7），versionCode 已在此前的日常修改中递增好
3. 更新 `CHANGELOG.md`：
   - 创建新版本条目，按类别（🐛修复/🚀性能/✨新功能/🔧其他）整理完整变更清单
4. 更新 `README.md`：
   - 在版本历史顶部创建新版本条目，同样按类别整理
5. 提交并推送至 `master` 和 `dev`
